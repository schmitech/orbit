# OrbitChat Secrets
# Only actual secrets belong here. All other settings go in orbitchat.yaml.

# Auth0 Authentication (required only if auth.enabled: true in orbitchat.yaml)
VITE_AUTH_DOMAIN=
VITE_AUTH_CLIENT_ID=
VITE_AUTH_AUDIENCE=

# Adapter configurations (JSON array with API keys — keys never reach the browser)
# Example:
# VITE_ADAPTERS='[{"name":"Simple Chat","apiKey":"default-key","apiUrl":"https://orbit.example.com"},{"name":"Chat With Files","apiKey":"multimodal","apiUrl":"https://orbit.example.com"}]'
