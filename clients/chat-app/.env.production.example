# Production Environment Configuration
# Copy this file to .env.production and configure for your environment

# =============================================================================
# ADAPTER CONFIGURATION (Required for middleware mode)
# =============================================================================
# JSON array of adapters - API keys and URLs are kept server-side only
# Format: [{"name":"Display Name","apiKey":"your-secret-key","apiUrl":"https://your-backend-api.com"}]
VITE_ADAPTERS='[{"name":"Simple Chat","apiKey":"your-api-key-here","apiUrl":"https://your-orbit-backend.com"}]'

# =============================================================================
# FEATURE FLAGS
# =============================================================================
VITE_ENABLE_API_MIDDLEWARE=true
VITE_ENABLE_UPLOAD=true
VITE_ENABLE_AUDIO_OUTPUT=false
VITE_ENABLE_CONVERSATION_THREADS=true
VITE_ENABLE_FEEDBACK=false
VITE_CONSOLE_DEBUG=false

# =============================================================================
# LIMITS (adjust based on your use case)
# =============================================================================
VITE_MAX_FILES_PER_CONVERSATION=5
VITE_MAX_FILE_SIZE_MB=50
VITE_MAX_TOTAL_FILES=100
VITE_MAX_CONVERSATIONS=10
VITE_MAX_MESSAGES_PER_CONVERSATION=1000
VITE_MAX_TOTAL_MESSAGES=10000
VITE_MAX_MESSAGE_LENGTH=1000

# =============================================================================
# SERVER CONFIGURATION (for orbitchat CLI / Docker)
# =============================================================================
PORT=3000
HOST=0.0.0.0

# =============================================================================
# NOTES FOR AWS DEPLOYMENT
# =============================================================================
# 1. Store VITE_ADAPTERS in AWS Secrets Manager or Parameter Store
# 2. Inject at runtime via ECS task definition or EC2 user data
# 3. Never commit actual API keys to version control
# 4. ALB health check path: /api/adapters (returns 200 if adapters configured)
