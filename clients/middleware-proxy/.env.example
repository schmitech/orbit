# Middleware Proxy Configuration
# Copy this file to .env and configure for your environment

# Server Configuration
PORT=3001
HOST=0.0.0.0

# Adapter Configuration (JSON array)
# Each adapter needs: name, apiKey, apiUrl
# Example:
ORBIT_ADAPTERS='[{"name":"Simple Chat","apiKey":"orbit_xxxx","apiUrl":"https://orbit-server.example.com"},{"name":"Advanced QA","apiKey":"orbit_yyyy","apiUrl":"https://orbit-server.example.com"}]'

# CORS Configuration
# Comma-separated list of allowed origins
# Use '*' for development only (not recommended for production)
ALLOWED_ORIGINS=https://app.example.com,https://widget.example.com

# Rate Limiting
# Window in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000
# Max requests per window per adapter (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# Logging
# Level: debug, info, warn, error (default: info)
LOG_LEVEL=info
# Format: json, pretty (default: json)
LOG_FORMAT=json
# Log all requests: true, false (default: true)
LOG_REQUESTS=true

# Optional: Config file path (overrides env vars for adapter config)
# CONFIG_FILE=/etc/middleware-proxy/config.json
