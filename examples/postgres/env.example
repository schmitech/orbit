# Environment variables for PostgreSQL connection
# The connection string will be constructed dynamically from these values

# AWS RDS Configuration
DATASOURCE_POSTGRES_USERNAME=postgresql
DATASOURCE_POSTGRES_PASSWORD=your_secure_password
DATASOURCE_POSTGRES_HOST=host
DATASOURCE_POSTGRES_PORT=5432
DATASOURCE_POSTGRES_DATABASE=your_database_name

# SSL Mode (recommended: require or verify-full)
# - require: Always use SSL
# - verify-full: Use SSL and verify server certificate
# - prefer: Use SSL if available
# - disable: No SSL (not recommended for production)
DATASOURCE_POSTGRES_SSL_MODE=require

# Ollama Configuration
# Base URL for Ollama server (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Ollama Models
# Embedding model for semantic search (default: nomic-embed-text)
OLLAMA_EMBEDDING_MODEL=bge-m3

# Inference model for natural language generation (default: gemma3:1b)
OLLAMA_INFERENCE_MODEL=gemma3:12b